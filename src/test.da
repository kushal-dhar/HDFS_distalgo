#config(clock is lamport)


class proc(process):
    
    def setup():
        pass
    
    def run():
        #-- helloasd
        output('waiting term from namenode')
        await(received(('x','y')))
        output('received term from namenode')
        
    def receive(msg= ('x', 'y')):
        output('received hello from ')
        #send('ack', to=sender)

def main():
    send(('start_datanodes',3,'dnode'), to='snode')